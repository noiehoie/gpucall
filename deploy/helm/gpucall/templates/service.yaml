apiVersion: v1
kind: Service
metadata:
  name: {{ include "gpucall.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "gpucall.fullname" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
